@model IEnumerable<HiQo.StaffManagement.Domain.EntitiesDTO.GradeDto>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<table class="table">
    <thead>
    <tr>
        <td>Name</td>
        <td>Level</td>
    </tr>
    </thead>
    <tbody class="tab-content">
    @foreach (var grade in Model)
    {
        <tr>
            <td>@grade.Name</td>
            <td>@grade.Level</td>
        </tr>
    }
    </tbody>
</table>
